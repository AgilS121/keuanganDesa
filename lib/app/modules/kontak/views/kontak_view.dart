import 'package:flutter/material.dart';

class KontakView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Kontak Page', style: TextStyle(fontSize: 20)),
    );
  }
}
